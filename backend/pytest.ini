[pytest]
# Backend modules are imported flat (`import db`, `import ev_model`), so the
# backend directory itself has to be on the path when the suite runs.
pythonpath = .
testpaths = tests
addopts = -q
